[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mlops-churn"
version = "0.1.0"
description = "Production-oriented customer churn prediction service"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]